using UnityEngine;

namespace SpecialAssignment
{
    public class ToolingNavigator : MonoBehaviour
    {
        public enum ToolingType
        {
            VoicePrintAnalysis = 0,
            LocationTracking = 1,
            FacialRecognition = 2,
        }

        [SerializeField] private ToolingType _toolingType;
        [SerializeField] private 
    }
}