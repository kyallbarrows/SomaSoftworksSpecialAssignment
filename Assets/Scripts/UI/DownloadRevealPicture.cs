using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SpecialAssignment
{
    // an image element that transitions in like a picture being downloaded over 56k modem, where it's fuzzy but 
    // it gets more clear, top to bottom.
    public class DownloadRevealPicture : MonoBehaviour
    {
        
        
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
